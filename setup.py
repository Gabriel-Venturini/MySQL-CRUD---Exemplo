# this file setup the packages of the app

from setuptools import setup, find_packages

setup(name='Basic_CRUD', version='1.2', packages=find_packages())

# run this on terminal with: pip install -e .